{
    "name": "becklyn/cert-key-matcher",
    "description": "Checks that a certificate / key / csr matches in a given directory.",
    "license": "BSD-3-Clause",
    "require": {
        "php": "^7.4",
        "symfony/console": "^5.0"
    },
    "config": {
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "Becklyn\\CertKeyChecker\\": "src/"
        }
    },
    "bin": [
        "bin/cert-key-checker"
    ]
}
